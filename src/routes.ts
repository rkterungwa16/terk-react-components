export enum ClientRoutes {
  HOME = "/",
  BASE = "/base",
  CARDS = "/base/cards",
  FORMS = "/forms",
  BUTTONS = "/buttons",
  NOTIFICATIONS = "/notifications",
  REGISTER = "/register",
  LOGIN = "/login",
  NOT_FOUND = "/404",
}
