export enum ClientRoutes {
  HOME = "/",
  BASE = "/base",
  FORMS = "/forms",
  BUTTONS = "/buttons",
  NOTIFICATIONS = "/notifications",
  REGISTER = "/register",
  LOGIN = "/login",
  NOT_FOUND = "/404",
}
